function areaTriangle(base, height) {
    return (b*h)/2;
}

function areaCircle(r) {
    return Math.PI * r * r;
}

function areaSqr(w, l) {
    return w * l;
}
exports = {areaTriangle, areaCircle, areaSqr};